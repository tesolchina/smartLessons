# Copy to .env and fill in your key
# Or set via shell: export XAI_API_KEY="..."
XAI_API_KEY=

# Optional overrides
XAI_API_BASE=https://api.x.ai/v1
GROK_MODEL=grok-4

# Optional paths
# Point to a file that contains only the API key
# XAI_API_KEY_FILE=/absolute/path/to/xai_api_key.txt

# Point to a specific .env file location
# XAI_DOTENV_PATH=/absolute/path/to/.env
